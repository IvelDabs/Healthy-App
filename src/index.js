// This app explores how to pass state informaton between two children of the parent component by the concpet of 'context API and state management with the useContext and useReducer hooks in react'

import React from 'react';
import {createRoot} from 'react-dom/client';
import App from './App';

createRoot(document.getElementById('root')).render(<App/>);
